# ──────────────────────────────────────────────────────
# JDE Mission Control — Environment Variables
# ──────────────────────────────────────────────────────
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# ──────────────────────────────────────────────────────
# PUBLIC VARIABLES (safe for client-side bundle)
# ──────────────────────────────────────────────────────
# These are the ONLY vars to add to Vercel:
#   Vercel Dashboard > Project Settings > Environment Variables
#   Environments: ✅ Preview  ✅ Production
#
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key

# ──────────────────────────────────────────────────────
# SECRET — server-only, NEVER add to Vercel Preview
# ──────────────────────────────────────────────────────
# ⚠️  The service role key BYPASSES all Row Level Security.
#     It is used ONLY by the local seed script:
#       npm run db:seed  (scripts/seed-demo-data.ts)
#
#     NO runtime code in /src references this key.
#     All server actions use cookie-based auth via anon key + RLS.
#
# ⚠️  DO NOT add to Vercel Preview — preview branches could leak it.
#     Only add to Vercel Production if you later build admin API
#     routes that explicitly require RLS bypass.
#
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# ──────────────────────────────────────────────────────
# OPTIONAL
# ──────────────────────────────────────────────────────
# Supabase project ID (for CLI: npm run db:types)
# SUPABASE_PROJECT_ID=your-project-id

# Explicit site URL override (Vercel auto-sets VERCEL_URL)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
