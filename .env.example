# ──────────────────────────────────────────────────────
# JDE Mission Control — Environment Variables
# ──────────────────────────────────────────────────────
# Copy this file to .env.local and fill in the values.
#
# Required for both dev and production (Vercel):
# ──────────────────────────────────────────────────────

# Supabase — Project URL & anon key (public, safe for client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Supabase — Service role key (server-only, NEVER expose to client)
# Used by: seed script, admin operations
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ──────────────────────────────────────────────────────
# Optional / Nice-to-have
# ──────────────────────────────────────────────────────

# Supabase project ID (for `npm run db:types`)
# SUPABASE_PROJECT_ID=your-project-id

# Site URL — used for auth redirect URLs
# In production Vercel sets VERCEL_URL automatically
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
